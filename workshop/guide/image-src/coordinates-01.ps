/rarc {
    [/b /a /r /dy /dx] dup length dict begin { exch def } forall
        currentpoint dy add exch dx add exch r a b arc
    end
} def

/p { % x y
    [/y /x] dup length dict begin { exch def } forall
        /d 10 def
        x y moveto
        d 0 rlineto
        0 d rlineto
        0 d d 270 90 rarc
        d neg 0 rlineto
        closepath
    end
} def

/page-width 148 def
/page-height 210 def

72 25.4 div dup scale
page-width 2 div page-height 2 div translate



newpath
0 0 p
0.5 setgray
stroke

5 10 translate

newpath
0 0 p
0 setgray
stroke


showpage